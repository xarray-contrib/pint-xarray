name: pint-xarray-tests
channels:
  - conda-forge
dependencies:
  - python=3.7
  - numpy
  - pint
  - xarray
  - pytest
  - pytest-cov
  - pytest-azurepipelines
  - coveralls
  - flake8
  - isort
  - black
  - pip
  - pip:
      - blackdoc